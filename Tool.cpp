#include"Tool.h"
/*using namespace std;
template<class T> void Shuffle<T>::run(const vector<T>&_things){
	int _size = _things.size();
	typename vector<T>::iterator _biter = _things.begin();
	T _temp;
	for(typename vector<T>::iterator iter = _biter; iter != _things.end(); iter++){
		typename vector<T>::iterator _riter = _biter+random(_size);  //保存临时随机迭代器
		_temp = *iter;
		*iter = *_riter;
		*_riter = _temp;
	}
}

template<class T> int Shuffle<T>::random(int _rang){
	srand((int)time(0));
	return rand()%_rang;
}*/
